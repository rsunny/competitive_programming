#include <iostream>

using namespace std;

bool vis[105][30];

int main(){
	int t;
	cin>>t;
	while(t--){
		vector<string> v;
		int n,m;
		cin>>n>>m;
		for(int i=0;i<n;i++){
			string temp;
			cin>>temp;
			v.push_back(temp);
		}
	}
	return 0;
}